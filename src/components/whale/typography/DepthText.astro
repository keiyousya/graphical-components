---
interface Props {
  tag?: "h1" | "h2" | "h3" | "h4" | "p" | "span";
  variant?: "display" | "ui" | "mono";
  class?: string;
}

const {
  tag: Tag = "h2",
  variant = "display",
  class: className = "",
} = Astro.props;

const variantStyles = {
  display: "font-display tracking-[0.15em] uppercase text-ink-700",
  ui: "font-ui tracking-wider text-ink-500",
  mono: "font-mono text-ink-400",
};
---

<Tag class:list={[variantStyles[variant], className]}>
  <slot />
</Tag>
